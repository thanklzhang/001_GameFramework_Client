using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EntityHpColorSelector : MonoBehaviour
{
    public Color selfBgColor;
    public Color friendBgColor;
    public Color enemyBgColor;

    public Color selfTextColor;
    public Color friendTextColor;
    public Color enemyTextColor;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
